# Modern Task Manager — Task List

> **Stack:** Next.js, TypeScript, Tailwind CSS, Supabase | **Repo:** https://github.com/Muhammad-Anique/modern-task-manager-32991
> Generated by Lead Engineer | Status: In Progress

---

## Tasks

- [ ] **Task 1: Initialize Next.js project skeleton**
  Create `package.json` with dependencies (next, react, react-dom, @supabase/supabase-js, lucide-react), `tsconfig.json`, `next.config.ts`, and `.gitignore`. Commit: `chore: initialize Next.js project skeleton`.

- [ ] **Task 2: Configure Tailwind CSS and Globals**
  Create `tailwind.config.ts`, `postcss.config.js`, and `app/globals.css` with Tailwind directives and base styling. Commit: `chore: configure Tailwind CSS`.

- [ ] **Task 3: Set up Supabase Client and Types**
  Create `lib/supabase.ts` for client initialization and `lib/types.ts` for the Task model. Create `.env.local` template (as `.env.example`). Commit: `chore: set up Supabase and types`.

- [ ] **Task 4: Create Database Migration**
  Create `supabase/migrations/20240101000000_create_tasks_table.sql` with the tasks table schema, check constraints, and initial RLS policy. Commit: `feat: add tasks table migration`.

- [ ] **Task 5: Implement Root Layout and Providers**
  Create `app/layout.tsx` with standard metadata, font optimization, and global style imports. Commit: `feat: add root layout`.

- [ ] **Task 6: Create StatusBadge and Shared Components**
  Create `components/StatusBadge.tsx` for visual status representation and any reusable atomic UI components. Commit: `feat: add shared UI components`.

- [ ] **Task 7: Build TaskCard and TaskBoard Components**
  Create `components/TaskCard.tsx` (item display/actions) and `components/TaskBoard.tsx` (the columnar Kanban layout). Commit: `feat: add TaskCard and TaskBoard`.

- [ ] **Task 8: Implement TaskModal for CRUD operations**
  Create `components/TaskModal.tsx` containing the form for creating and editing tasks. Commit: `feat: add TaskModal form`.

- [ ] **Task 9: Build Main Dashboard View**
  Implement `app/page.tsx` integrating the TaskBoard with data fetching from Supabase and real-time subscription setup. Commit: `feat: implement dashboard page`.

- [ ] **Task 10: Write README**
  Create a comprehensive README.md at the repository root. Include: project title, features, tech stack, database schema, and setup instructions. Commit: `docs: add comprehensive README`.